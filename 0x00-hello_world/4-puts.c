#include<stdio.h>
/**
 *Prints Programming is like building a multilingual puzzle
 * and after it will return 0
 */
int main (void)
{
puts("\"Programming is like building a multilingual puzzle");
return(0);
}
